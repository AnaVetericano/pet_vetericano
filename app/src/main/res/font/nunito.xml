<?xml version="1.0" encoding="utf-8"?>
<!--
    Familia Nunito para texto general (reemplaza a @font/sans).
    Normal = Regular (400) | Negrita = Bold (700).
    Nunito es la misma tipografía del panel web (Angular).
-->
<font-family xmlns:android="http://schemas.android.com/apk/res/android">
    <font
        android:font="@font/nunito_regular"
        android:fontStyle="normal"
        android:fontWeight="400" />
    <font
        android:font="@font/nunito_bold"
        android:fontStyle="normal"
        android:fontWeight="700" />
</font-family>
